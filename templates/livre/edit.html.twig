{% extends 'base.html.twig' %}

{% block title %}Edit Livre{% endblock %}

{% block body %}
    <h1 class="pt-6 pb-6 text-center uppercase font-bold text-xl">Edit Livre</h1>
    <div class="text-center px-2  ">
        {{ include('livre/_form.html.twig', {'button_label': 'Update'}) }}

        <a class="no-underline bg-purple-800 rounded-full my-2  p-1 text-white" href="{{ path('livre_index') }}">back to list</a>

        {{ include('livre/_delete_form.html.twig') }}
    </div>
{% endblock %}
